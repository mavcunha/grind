# vim: ft=sh
use "core"

function brew_pkg() {
  local formula="${1}"
  local short=${formula##*/}
  log "brew_pkg: ${formula} | short formula: ${short}"
  do_run "brew install ${formula}"
    unless "brew list --versions ${short} &> /dev/null"
}
