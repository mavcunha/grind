# vim: ft=sh

in_yellow "dry-run mode enabled\n"
# during a dryrun we override
# the execution function for a NOOP
function __run() {
  log "__run (noop): ${__GRIND_CMD_NEXT}"
  __magenta_box "NOOP"
}
